node_modules/
dist/
*.tgz
.vite/
coverage/
.DS_Store
scripts/.consumer-tmp/
packages/mcp/.typecheck-scaffold/

# Claude Code local (personal) settings
.claude/settings.local.json
.claude/launch.json
.git.root-backup/

# Local planning docs — personal, kept OUT of the repo/remote AND the MCP corpus
# (the corpus globs top-level docs/*.md; a subdir is not globbed, so plans live here)
docs/local/
